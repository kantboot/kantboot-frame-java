# KANTBOOT 框架

## 概述
KANTBOOT 是一个基于 Spring Boot 的应用开发框架。目前属于内部使用阶段。

## 版本信息
`1.0.0`

## 构建与部署

## 依赖版本管理
> 使用本框架的项目，建议使用${kantboot.version}来管理版本号
> 
> 框架中，必须使用${revision}管理版本号
> 
> ${kantboot.version}与${kantboot.version}同源，所以值一致
